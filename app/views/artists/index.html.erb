<h1>Artists Index Page</h1>

<ul>
<% @artists.each do |artist| %>
    <li>
    <%= link_to artist.name, artist_path(artist)%>
    <%= artist.songs.count %> <%= "song".pluralize(artist.song_count) %>
    </li>
<% end %>
</ul>