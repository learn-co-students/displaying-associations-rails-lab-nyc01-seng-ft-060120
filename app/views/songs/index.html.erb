<h1> Songs </h1>
<ul>
<% @songs.each do |song| %>
    <li>
    <%= song.artist_name %> - <%= link_to song.title, song_path(song.id) %>
    </li>
<% end %>
</ul><br>

<%= link_to 'Create Song', new_song_path %>